FUNC_GET_SUBDIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR} SUBDIRS)
foreach(SUBDIR ${SUBDIRS})
    add_subdirectory(${SUBDIR})
endforeach()

set(VAR_EXPOSED_SOURCES ${VAR_EXPOSED_SOURCES} ${SOURCES_IN_CURRENT_DIR} PARENT_SCOPE)
